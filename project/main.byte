void : term(i32 exit_code : [reg(x0)], i32 test_num : [stack]) {
    asm(format("
        mov x16, #1
        svc #0x80
    "));
}

void : main() {
    i32 var = 5;
    i16 var2 = 2;
    i8 var3 = 1;
    i32 exit_code = 0;

    bl(term, exit_code, var);
}
